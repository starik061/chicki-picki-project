$(document).ready(function () {
  $(".slider").slick({
    slidesToShow: 2,
    slidesToScroll: 1,
    infinite: true,
    centerMode: true,
    centerPadding: "85px",
    // autoplay: true,
    // autoplaySpeed: 1500,
  });
});